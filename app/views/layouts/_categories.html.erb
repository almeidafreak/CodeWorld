<br>

<table>
  <tbody>
    <% @categories.each do |category| %>
      <div class="entry">
        <%= link_to (image_tag category.image, :class=>"itemholsIm"), category %>
        <h3><%= category.title %></h3>
        <p><%= sanitize(category.description) %></p>
          
          <% @title = category.title %>
          <span>&emsp;</span><span>&emsp;</span><span>&emsp;</span>
          <span>&emsp;</span><span>&emsp;</span><span>&emsp;</span>
          <span>&emsp;</span>
          <span class="button"><%= link_to "Check " + @title + " products", 
              :controller => 'static_pages', :action => 'category', :title => @title %></span>
          
      </div>
    <% end %>
  </tbody>
</table>
